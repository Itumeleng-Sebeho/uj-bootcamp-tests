describe('test count my reg number function', function () {
    it("it should return 'false' when called with 'CJ 812328' ", function () {
        // assert.equal(regCount, 3);
    })
    it("it should return 'false' when called with 'CY 523519' ", function () {
        // assert.equal(regCount, 3);             
    })
    it("it should return '3' when called with 'CA 182736' ", function () {
        // assert.equal(regCount, 3);      
    })
});
