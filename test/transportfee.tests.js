describe('transport fee test', function () {
    it("it should return 'R20' when called with 'morning' ", function () {
        // assert.equal("CY", regNum.startsWith("CY"), "This should not be true!");

    });
    it("it should return 'R10' when called with 'afternoon' ", function () {
        // assert.equal("CY", regNum.startsWith("CY"), "This should not be true!");

    });
    it("it should return 'Free' when called with 'default' ", function () {
        // assert.equal("CY", regNum.startsWith("CY"), "This should not be true!");
    
    });
    it("it should return 'R15' when called with 'public holidays' ", function () {
        // assert.equal("CY", regNum.startsWith("CY"), "This should not be true!");
    
});
});