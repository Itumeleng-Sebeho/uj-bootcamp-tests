describe('from where exactly test', function () {
    it("it should return 'CY' when called with 'Bellville' ", function () {
        // assert.equal("CY", regNum.startsWith("CY"), "This should not be true!");

    });
    it("it should return 'CA' when called with 'Cape Town' ", function () {
        // assert.equal("CY", regNum.startsWith("CY"), "This should not be true!");

    });
    it("it should return 'GP' when called with 'Gauteng' ", function () {
        // assert.equal("CY", regNum.startsWith("CY"), "This should not be true!");
    
    });
    it("it should return 'NW' when called with 'North West' ", function () {
        // assert.equal("CY", regNum.startsWith("CY"), "This should not be true!");

    });
});